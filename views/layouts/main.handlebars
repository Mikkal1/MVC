<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
      <header class="hero">
          <div>
            <nav class="nav">
              <a id="home" href="/">Home</a>
              <a id="dashboard" href="/dashboard">Dashboard</a>
              {{#if loggedIn}}
              <a id="logout" href="/" id="logout">Logout</a>
              {{else}}
              <a id="login" href="/login">Login</a>
              {{/if}}
            </nav>          
          </div>

          <h1 class="app-title"><i class="fab"></i>
            {{#if loggedIn}} 
            The Dashboard
            {{else}}         
            The Tech Blog
            {{/if}}
          </h1>
      </header>

    <main>
      {{{ body }}}
    </main>
  </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}

</body>

</html>